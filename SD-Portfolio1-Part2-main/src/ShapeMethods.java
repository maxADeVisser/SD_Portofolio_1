//Alle vores klasser SKAL have følgende metoder

public interface ShapeMethods {
    double computeAreal();
    double computeCircumference();
    void computeCenter();
    boolean checkPoint(double x, double y);
}
